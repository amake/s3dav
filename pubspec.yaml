name: dav_s3_gateway
description: Minimal authenticated HTTP file gateway backed by Amazon S3.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

dependencies:
  crypto: ^3.0.6

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
